# Pinned to versions that install together (LangChain 1.x, 2026-09-16).
# The 0.3.x line is end-of-life and never received the langchain-core path-traversal
# fix (CVE-2026-34070), so this recipe now tracks LangChain 1.x. Versions match the
# set already locked by 05-agentic-rag/langgraph-local-models.
# LangChain ecosystem - pinned to working versions
langchain-classic==1.0.8     # RetrievalQA and the other legacy chains moved here in 1.x
langchain-community==0.4.2
langchain-core==1.6.3
langchain-db2==1.0.0
langchain-text-splitters==1.1.2
langchain-ibm==1.1.0
ibm_watsonx_ai==1.6.0        # imported directly by rag-basic.ipynb
pydantic==2.13.4             # imported directly by rag-basic.ipynb
langgraph==1.2.11

# LLM and embeddings - pinned to working version
llama-cpp-python==0.3.14

# Database
ibm-db==3.2.9

# Document processing
trafilatura==2.1.0
spacy==3.8.14

# Utilities
python-dotenv==1.2.2
tiktoken==0.13.0
huggingface-hub==1.25.1

# Jupyter environment
ipykernel==7.3.0
jupyter==1.1.1
